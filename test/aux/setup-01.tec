! Start of initialization !

0,128 ET                                ! Abort if error occurs !

EO - 100 "L
    -1 EJ - 1024 "E                     ! TECO-32 !
        1 UZ                            ! -P !
    |                                   ! Unknown or unsupported TECO !
        0 UZ                            ! Assume no -P or ext. features !
    '
    13@^UY// 10:@^UY//                  ! Set up macro to insert CR/LF !
|
    EO - 200 "L                         ! TECO C !
        0 UZ                            ! No -P !
        10@^UY//                        ! Set up macro to insert LF !
    |                                   ! TECO-64 !
        3 UZ                            ! -P, ext. features !
        10@^UY//                        ! Set up macro to insert LF !
    '
'

@^UZ/ ^D 13^T 10^T/                     ! Set up macro to print CR/LF !

@^A/Starting TECO v/ EO:= @^A/ test/ MZ

HK                                      ! Clear the edit buffer !

! Start of test !
